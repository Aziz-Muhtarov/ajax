const screenSizeBtn = document.querySelector(".arrow");
const screenWidth = window.innerWidth;
const screenHeight = window.innerHeight;


function result () {
    return alert(`Screen width: ${screenWidth}px\nScreen height: ${screenHeight}px`);
}




// document.addEventListener('DOMContentLoaded', function() {
//     const screenSizeButton = document.getElementById('screenSizeButton');

//     screenSizeButton.addEventListener('click', function() {
//         const screenWidth = window.innerWidth;
//         const screenHeight = window.innerHeight;

//         alert(`Screen Width: ${screenWidth}px\nScreen Height: ${screenHeight}px`);
//     });
// });